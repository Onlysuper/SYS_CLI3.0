import Base from "@src/apis/base"
const Layout = {
    state: {
        oaIp: Base.oaIp, // true为高级搜索 false为普通搜索
        imgIP: Base.imgIP,
        merIp: Base.merIp,
    },
    getters: {
    },
    mutations: {

    },
    actions: {
    },
    modules: {
    }
};

export default Layout